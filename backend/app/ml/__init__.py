"""Machine learning (PRD §10).

Four surfaces, each with its own lifecycle: risk classification (FR-03), market
prediction (FR-08), recommendation (M4) and portfolio optimization (M4). Every
served result carries the `model_version` that produced it (§10.5).
"""
